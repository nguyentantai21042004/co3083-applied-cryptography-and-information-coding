| #  | Question | Answer | Giải thích |
|----|----------|--------|------------|
| 1 | What is a collision in the context of hash functions? | A. When two different inputs produce the same hash output | Va chạm xảy ra khi hai giá trị đầu vào khác nhau ($x \neq y$) nhưng có cùng giá trị băm: $H(x) = H(y)$. |
| 2 | According to the birthday paradox, how many people do you need for a 50% chance of a shared birthday? | A. 23 people | Định lý Sinh nhật cho thấy chỉ cần 23 người để đạt xác suất 50% có hai người trùng sinh nhật. Điều này minh họa khả năng va chạm xuất hiện sớm hơn dự đoán (khoảng $\sqrt{N}$ với $N$ là số khả năng). |
| 3 | What is the purpose of adding salt to password hashing? | B. To prevent precomputation attacks like rainbow tables | "Salt" là giá trị ngẫu nhiên, duy nhất được thêm vào mật khẩu trước khi băm, khiến bảng Rainbow trở nên vô tác dụng do phải tính toán lại cho mỗi trường hợp. |
| 4 | Which hash function construction is vulnerable to length extension attacks? | C. Merkle-Damgård (used in SHA-2) | Cấu trúc Merkle-Damgård (vd. MD5, SHA-1, SHA-2) dễ bị tấn công mở rộng độ dài vì trạng thái đầu ra cuối cùng được sử dụng tiếp như đầu vào. |
| 5 | What researcher demonstrated the first practical MD5 collision in 2004? | A. Xiaoyun Wang | Xiaoyun Wang là nhà nghiên cứu Trung Quốc đã trình diễn va chạm MD5 thực tế đầu tiên năm 2004. |
| 6 | What is HMAC designed to protect against? | B. Length extension attacks | HMAC sử dụng việc băm lồng nhau và khóa bí mật để bảo vệ khỏi tấn công mở rộng độ dài—a điểm yếu của Merkle-Damgård. |
| 7 | In the sponge construction, what prevents length extension attacks? | C. Hiding part of the internal state (capacity) from the output | Cấu trúc Sponge (như SHA-3) bảo vệ bằng cách giữ kín phần capacity của trạng thái nội bộ, không cho xuất hiện trong đầu ra. |
| 8 | What was the name of the 2017 SHA-1 collision attack by Google? | C. SHAttered | Google và CWI Amsterdam công bố tấn công va chạm SHA-1 thật đầu tiên vào năm 2017 với tên gọi "SHAttered". |
| 9 | Why is storing passwords in plaintext dangerous? | A. Attackers immediately get all passwords if the server is compromised | Nếu dữ liệu bị lộ, toàn bộ mật khẩu sẽ bị rò rỉ ngay lập tức. |
|10| What makes Scrypt a memory-hard function? | A. It requires storing a large array of pseudorandom values during computation | Scrypt yêu cầu bộ nhớ lớn trong quá trình tính toán, gây khó khăn cho phần cứng chuyên dụng với bộ nhớ hạn chế (ASIC, GPU). |
|11| Approximately how many SHA-256 hashes can a high-end GPU compute per second? | A. 2+ billion hashes/second | GPU hiện đại có thể tính hơn 2 tỉ hàm băm mỗi giây, nên cần dùng hàm băm mật khẩu chuyên dụng để tránh tấn công brute-force. |
|12| What is PBKDF2? | A. A key derivation function that iterates a PRF many times | PBKDF2 là hàm tạo khóa sử dụng một hàm giả ngẫu nhiên lặp lại nhiều vòng để làm chậm quá trình băm. |
|13| What property must collisions have due to the pigeonhole principle? | B. They must exist when infinite inputs map to finite outputs | Theo Nguyên lý Lỗ Bồ Câu: tập đầu vào vô hạn, tập đầu ra hữu hạn thì chắc chắn tồn tại va chạm. |
|14| What is the Random Oracle Model? | C. A theoretical framework modeling hash functions as truly random functions | ROM lý thuyết hóa hàm băm như hộp đen trả về giá trị ngẫu nhiên lý tưởng, dùng trong chứng minh bảo mật giao thức. |
|15| Why is encrypting passwords (instead of hashing) a bad idea? | C. The server needs the decryption key, which an attacker could also steal | Nếu máy chủ bị tấn công, kẻ xấu lấy cả mật khẩu mã hóa lẫn khóa giải mã, khiến giải pháp mã hóa thất bại. |
|16| What makes rainbow tables ineffective against salted hashes? | A. Each unique salt requires a completely new rainbow table | Mỗi salt duy nhất buộc kẻ tấn công phải xây dựng lại bảng Rainbow, khiến tấn công không còn thực tế. |
|17| What is protocol agility in cryptography? | C. The ability to support and transition between multiple cryptographic algorithms | Tính linh hoạt cho phép giao thức chuyển sang thuật toán mới khi phát hiện ra lỗ hổng ở thuật toán cũ. |
|18| How much computational effort did the SHAttered attack require? | A. About 6,500 CPU years and 110 GPU years | SHAttered tiêu tốn khoảng 6.500 năm tính CPU và 110 năm GPU, nhanh hơn brute-force ngẫu nhiên hàng chục nghìn lần. |
|19| What discovery about Joux's multi-collision attack was surprising? | A. Finding $2^n$ collisions only takes about $n$ times the work of finding one collision | Joux chứng minh việc tạo nhiều va chạm ($2^n$) chỉ tốn gấp $n$ lần so với tìm một va chạm, ít hơn dự đoán trực giác rất nhiều. |
|20| Why does Bitcoin mining demonstrate hash function security? | B. The extreme computational cost shows how hard it is to find specific hash patterns | Đào Bitcoin đòi hỏi chi phí tính toán cực lớn để tìm đầu ra hàm băm đạt tiêu chí—minh họa thực tế cho việc hàm băm an toàn. |